services:
  osint:
    build: .
    ports:
      - "8443:8443"
    volumes:
      - osint-data:/app/data
      - osint-reports:/app/reports
      - osint-certs:/app/certs
    restart: unless-stopped

volumes:
  osint-data:
  osint-reports:
  osint-certs:
